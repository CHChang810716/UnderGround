if( ${CMAKE_CXX_COMPILER_ID} STREQUAL "GNU" )
    execute_process( COMMAND ${CMAKE_CXX_COMPILER} -dumpversion OUTPUT_VARIABLE GCC_VERSION )
    if( NOT (GCC_VERSION VERSION_GREATER 6.0 OR GCC_VERSION VERSION_EQUAL 5.0) )
        message( FATAL_ERROR "${PROJECT_NAME} requires g++ 6.0 OR greater." )
    endif()
    set( CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++17" )
elseif( ${CMAKE_CXX_COMPILER_ID} STREQUAL "Clang" )
    set( CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++1z" )
elseif( ${CMAKE_CXX_COMPILER_ID} STREQUAL "MSVC" )
    message( WARNING "MSVC is currently unstable, suggest use MinGW" )
else()
    message( FATAL_ERROR "Your C++ compiler does not support C++17." )
endif()
